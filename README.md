# Rocket chat breakout room generator

... or something like that.

## Install

```
virtualenv venv
. ./venv/bin/activate
pip install -r requirements.txt
cp .env.sample .env
```

Then, fill in your credentials in .env.

## Run

Start with

```
python generate.py
```
